# Only fail on high or critical severity.
# The 9 moderate issues are in Google SDK transitive deps (uuid inside
# firebase-admin/gaxios/google-gax). Our code never calls uuid directly.
# Google will patch this in their own SDK release.
audit-level=high
